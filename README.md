# 📂 OpenList Mobile
- 支持Alist/OpenList 服务器
  SUPPORT ALIST AND OPENLIST SERVERS

---

## ✨ 主要功能 / Key Features

### 📁 存储管理 / Storage Management
- **多服务器管理：** 支持保存多个服务器历史，下拉菜单快速切换.
  **Multi-Server:** Manage multiple server configs with quick-switch history.
- **智能排序：** 支持文件夹置顶，按名称、日期或大小灵活排序。  
  **Smart Sorting:** Folders-first logic with flexible sorting options.
- **文件过滤：** 一键筛选视频、图片、文档 or 自定义后缀名。  
  **Filtering:** Quick filters for videos, images, documents, or custom extensions.
- **基础操作：** 支持文件多选，上传、下载、删除（带确认弹窗）。  
  **Core Ops:** Secure upload, deletion, and basic file management.

### 🎥 预览播放 / Media & Preview
- **外部播放器：** 支持调用 nPlayer, VLC, MX Player 进行 4K 播放.
  **External Players:** Stream via nPlayer, VLC, or MX Player with one tap.
- **全能预览：** 内置图片浏览器、PDF 阅读器及代码/文本预览.
  **Rich Preview:** Built-in viewer for images, PDFs, and syntax-highlighted code.
- **直链获取：** 快速生成并复制原始直链.
  **Direct Links:** Generate and copy direct download links instantly.

---

## 🚀 快速开始 / Getting Started

1. **访问地址：** 在移动端浏览器中打开本项目部署链接.
   **Access:** Open the app URL in your mobile browser.
2. **连接配置：** 输入 Alist/OpenList 服务器地址（含端口）、用户名及密码.
   **Connect:** Enter your Alist/OpenList URL, username, and password.
3. **安全体验：** 登录后服务器信息将安全保存在本地.
   **Enjoy:** Your configuration is saved locally for quick access.

---

## 🛡️ 安全提示 / Security Notes

- **HTTPS 限制：** 若应用运行在 HTTPS 环境，Alist/OpenList 服务器也必须启用 HTTPS。  
  **HTTPS:** If the app uses HTTPS, your Alist/OpenList server must also use HTTPS.
- **跨域设置：** Alist/OpenList 后台需将 `允许跨域` (Allow Origins) 设置为 `*` 或您的域名。  
  **CORS:** Set `Allow Origins` to `*` or your domain in Alist/OpenList settings.

---